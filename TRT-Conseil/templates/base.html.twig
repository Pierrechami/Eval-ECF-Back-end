<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Welcome!{% endblock %}</title>
        <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>⚫️</text></svg>">
        {# Run `composer require symfony/webpack-encore-bundle` to start using Symfony UX #}
        {% block stylesheets %}
          {#  {{ encore_entry_link_tags('app') }}#}

            <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet"
                  integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">
            <link rel="stylesheet" href="{{ asset('style/css.css') }}">
        {% endblock %}

        {% block javascripts %}
          {#  {{ encore_entry_script_tags('app') }}#}
        {% endblock %}
    </head>
    <body class="container">



{% block herder %}
    <nav class="navbar navbar-expand-lg navbar-light bg-light">
        <div class="container-fluid">
            <a class="navbar-brand" href="{{ path('app') }}">Mes supper recette 👨🏻‍🍳 </a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent"
                    aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarSupportedContent">
                <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                    {% if is_granted('ROLE_ADMIN') %}
                        <li class="nav-item">
                            <a class="nav-link active" aria-current="page" href="{{ path('recipe_index') }}">Accées gestion recette</a>
                        </li>
                    {% endif %}
                    <li class="nav-item">
                        <a class="nav-link" href="#">Link</a>
                    </li>
                </ul>
            </div>
            <div>
                {% if is_granted('ROLE_ADMIN') %}
                    <p>sex : {{ app.user.sexx }}</p>
                    <p>Admin : {{ app.user.fullname }}  |
                        <a  class="link-danger" href="{{ path('logout') }}">Déconnection</a>
                    </p>
                {% elseif is_granted('ROLE_PROF') %}
                    <p>Prof : {{ app.user.fullname }}  |
                        <a class="link-danger" href="{{ path('logout') }}">Déconnection</a>
                    </p>
                {% elseif is_granted('ROLE_USER') %}
                    <p>sex : {{ app.user.sexx }}</p>
                    <p>Utilisateur : {{ app.user.fullname }} |
                        <a class="link-danger" href="{{ path('logout') }}">Déconnection</a>
                    </p>
                {% else %}
                    {{include('login/index.html.twig') }}
                {% endif %}
            </div>
        </div>
    </nav>
{% endblock %}

        {% block body %}

        {% endblock %}
    </body>
</html>


