{% extends 'base.html.twig' %}

{% block title %}Modifier le poste{% endblock %}

{% block body %}
    <h1>Modifier le contenue du poste</h1>
<div>
   <p>{{ include('job/_form.html.twig', {'button_label': 'Mettre à jour'}) }}</p>
 <p>{{ include('job/_delete_form.html.twig') }}</p>

</div>

    <a href="{{ path('job_index') }}" class="float-end">Retour à la liste</a>


{% endblock %}
